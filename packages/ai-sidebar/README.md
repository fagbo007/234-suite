# @234/ai-sidebar

Shared AI sidebar component for all three apps.

**Status:** reserved placeholder. Scaffold in **Phase 2** (collapsible, user-invoked,
no AI content); AI features go live in **Phase 3** (root `CLAUDE.md` Section 9).

> **The AI sidebar rule (Section 6):** AI lives in a collapsible sidebar only. It
> never floats over content, never speaks first, and is only ever user-invoked.
> This rule cannot be overridden without a decision recorded in
> `/docs/architecture/ai-sidebar.md`. AI is always optional.
