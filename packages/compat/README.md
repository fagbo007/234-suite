# @234/compat

MS Office round-trip compatibility layer for `.docx`, `.xlsx`, and `.pptx`.

**Status:** reserved placeholder. Implemented in **Phase 3** (root `CLAUDE.md`
Section 9). Imports must always complete and report fidelity losses via a
user-visible import report — never silently mangle data (Section 7, Section 16).
