// MS Office import/export compatibility layer (.docx, .xlsx, .pptx).
//
// Built in Phase 3. Every import must complete successfully and log fidelity
// losses to a user-visible import report — never silently mangle or discard
// content (root CLAUDE.md Section 7, Section 16). Placeholder until then.
export const COMPAT_PLACEHOLDER = true;
